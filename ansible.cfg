[defaults]
# Silence the "discovered Python interpreter" warning. This role targets a mix
# of distributions with differing Python locations, so let Ansible auto-detect
# the interpreter on each host rather than pinning ansible_python_interpreter.
interpreter_python = auto_silent
